server:
  port: 8080
  servlet:
    context-path: /springboot_study

spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/springboot_study'
    username: root
    password: rootroot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true